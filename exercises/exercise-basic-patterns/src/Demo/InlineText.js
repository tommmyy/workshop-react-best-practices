/* eslint-disable */
import React from 'react';
import { Text } from 'theme-ui';

// - Defaulting props - defaultProps - `as` span
// - Class vs Function
// - Arrow vs. Function
// - prop-types
// - forwarding - pass the `ref` as fn and log it
const InlineText = ({ children }) => children;

export default InlineText;
